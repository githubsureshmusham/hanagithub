ID: cloudmta1
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: clouddbmodule1
   type: hdb
   path: clouddbmodule1
   requires:
    - name: hdi_clouddbmodule1

resources:
 - name: hdi_clouddbmodule1
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
